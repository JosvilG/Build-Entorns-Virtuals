using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class IniciaJoc : MonoBehaviour
{

    /*public GameObject player;
    public static bool iniciat = false;
    Text tutorial;
    public GameObject panel;
    bool continuar;
    // Start is called before the first frame update
    void Start()
    {
        tutorial = GetComponent<Text>();
        
        if (iniciat == false)
        {
            player.SetActive(false);
            tutorial.GetComponent<Text>().enabled = true;
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space) && iniciat == false)
        {
            tutorial.GetComponent<Text>().enabled = false;
            player.SetActive(true);
            panel.SetActive(false);
            iniciat = true;
        }
    }*/
}
